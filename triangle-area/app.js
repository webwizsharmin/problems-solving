const b = prompt("Please enter the base of triangle: ");  //Declare variable for base & it will collect the value from the user

const h = prompt("Please enter the height of triangle: ");  // Declare the height

const area = 0.5 * b * h;

console.log(`The area is: ${area}`);